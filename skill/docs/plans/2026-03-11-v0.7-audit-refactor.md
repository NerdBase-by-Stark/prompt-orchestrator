# v0.7.0 Audit Refactor Plan

## Goal

Address all findings from AUDIT-REPORT-v0.6.0.md: 8 CRITICAL (C1 downgraded), 6 HIGH, 4 DRIFT, 17 GAPS, 14 IMPROVEMENTS. Bump version to 0.7.0. Preserve all 10 identified strengths (S1-S10).

## Architecture Decisions

1. `assets/templates/` becomes the single source of truth for all template content (D1-D4 resolution)
2. `references/` files become pure guidance documents -- zero template content, only rules/examples/heuristics
3. All placeholders standardized: `{{DOUBLE_BRACE}}` for orchestrator-time, `{SINGLE_BRACE}` for PM-time
4. Phase 0 renamed to "Detection Confidence" (separate identity from Phase 1.4 "Complexity Score")
5. Spawn prompt hardened with role sentence, all 4 statuses, file-scope restriction, extraction discipline reminder
6. `<extracted-source>` XML wrapping for clarity (not security hardening per user decision on C1)
7. Recursion guard uses HTML comment sentinel `<!-- ORCHESTRATOR_SUBAGENT_ACTIVE_v0.7 -->`

## File Inventory (Pre-Refactor)

| File | Action |
|------|--------|
| `SKILL.md` | Major edit (Group B) |
| `assets/templates/pm-orchestration.md` | Major edit (Groups A, C) |
| `assets/templates/task.md` | Edit (Group C) |
| `assets/templates/task-manifest.md` | Edit (Group C) |
| `assets/templates/context.md` | Edit (Group C) |
| `assets/templates/suggestions.md` | Edit (Groups A, C) |
| `references/pm-orchestration-template.md` | Rewrite to guidance-only (Group A) |
| `references/suggestions-template.md` | Rewrite to guidance-only (Group A) |
| `references/capability-inference.md` | Edit (Group D) |
| `references/extraction-examples.md` | Edit (Group D) |
| `references/workflow-patterns.md` | Edit (Group D) |
| `references/pm-detection-examples.md` | Minor edit (Group B) |
| `scripts/analyze_prompt.py` | Edit (Group E) |
| `assets/examples/PM-ORCHESTRATION.md` | Rewrite (Group F) |
| `assets/examples/CONTEXT.md` | Rewrite (Group F) |
| `assets/examples/subagent-tasks/*.md` | Rewrite (Group F) |

## Task Groups

---

### Group A: Drift Resolution (D1-D4) -- MUST BE FIRST

These tasks restructure the file hierarchy so that all subsequent groups edit the correct canonical files.

#### Task A1: Convert `references/pm-orchestration-template.md` to guidance-only

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/references/pm-orchestration-template.md`

**Current state**: 209 lines. Contains full template content in markdown code blocks duplicating `assets/templates/pm-orchestration.md`. Has 5 sections the asset template lacks (documented in D2), but the asset template has 5 sections the reference lacks.

**Action**: Replace entire file content with a guidance-only document. Remove ALL template content (the markdown code blocks containing `## PM RULES`, `## CONTEXT DISCIPLINE`, `## WHY SUBAGENTS ARE MANDATORY`, `## NON-NEGOTIABLES`, `## EXECUTION INSTRUCTIONS`, `## TASK SEQUENCE TABLE FORMAT`, `## PROGRESS TRACKER FORMAT`, `## ERROR HANDLING`).

**New content structure**:
```
# PM-ORCHESTRATION.md Generation Guide

> Reference file for prompt-orchestrator skill.
> Load when generating PM-ORCHESTRATION.md output.
> 
> **Template location**: `assets/templates/pm-orchestration.md` (single source of truth)

---

## Purpose

PM-ORCHESTRATION.md is the PM's execution playbook. It must contain everything the PM needs to coordinate subagents WITHOUT reading task files or source documents.

## Required Sections Checklist

Every generated PM-ORCHESTRATION.md MUST include these sections (template provides structure):

1. Header with project name, timestamp, complexity score, task count, source path
2. SUGGESTIONS AVAILABLE - counts by severity
3. AGENT ALLOCATIONS - summary from TASK-MANIFEST.md
4. PM RULES (READ FIRST) - 7 rules
5. CONTEXT DISCIPLINE - PM reads vs PM does not read, consequence table
6. EXTRACTION-BASED WORKFLOW - subagent instructions for verbatim execution
7. TASK SEQUENCE - order/file/description/blocker/parallel/agent table
8. FILES GENERATED - every file path created during orchestration [NEW in v0.7]
9. EXECUTION INSTRUCTIONS - per-task steps, parallel execution rules
10. PROGRESS TRACKER - status table
11. VALIDATION CHECKLIST - post-completion checks
12. EXTRACTION COVERAGE - source section mapping with totals
13. WHY SUBAGENTS ARE MANDATORY - reasoning table
14. NON-NEGOTIABLES - 8 absolute rules with anti-rationalization phrases
15. ERROR HANDLING - failure/timeout/blocked procedures
16. FINAL OUTPUT - complete/incomplete report formats

## Generation Rules

- Populate ALL `{{DOUBLE_BRACE}}` placeholders during orchestration
- The `{SINGLE_BRACE}` placeholders (only `{TASK_FILE}`) are filled by the PM at dispatch time
- Task descriptions in TASK SEQUENCE must be < 80 chars, no imperative language (H4)
- VALIDATION CHECKLIST must list concrete checks extracted from source (I7)
- FILES GENERATED section must list every file path the orchestrator created (C7)

## Positive Framing Guide (H3)

When writing PM instructions, prefer positive framing:

| Instead of | Write |
|------------|-------|
| "DO NOT READ task files" | "Your ONLY interaction with task files is pointing subagents at their file paths" |
| "DO NOT read the source" | "The source has been fully extracted -- the TASK SEQUENCE table has every detail you need" |
| "DO NOT execute tasks" | "Your role is dispatching agents and tracking results" |

Keep the negative reinforcement in NON-NEGOTIABLES (it works there as a backstop), but lead with positive framing in the main instruction sections.
```

**Resolves**: D2 (drift between reference and asset template)

---

#### Task A2: Convert `references/suggestions-template.md` to guidance-only

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/references/suggestions-template.md`

**Current state**: 109 lines. Contains full template in a markdown code block (lines 10-83) duplicating `assets/templates/suggestions.md`. Also contains useful severity classification and observation type guidance (lines 85-109).

**Action**: Remove the template code block (lines 10-83). Keep and expand the guidance sections (severity classification, observation types). Add new guidance for v0.7 features.

**New content structure**:
```
# SUGGESTIONS.md Generation Guide

> Reference file for prompt-orchestrator skill.
> Load when generating SUGGESTIONS.md output.
>
> **Template location**: `assets/templates/suggestions.md` (single source of truth)

---

## Purpose

SUGGESTIONS.md captures orchestrator observations that are ADVISORY ONLY. Suggestions NEVER appear in task files. If a suggestion is accepted, the source document must be amended first -- task files are not modified directly (H5).

## Severity Classification

[KEEP existing table from lines 87-94]

## Observation Types

[KEEP existing table from lines 100-109]

## Ambiguous Content Detection (NEW - v0.7, C1)

During Phase 1.3, if extracted content contains language that could be confused with instructions to the executing agent (e.g., "you should", "make sure to", "always do X"), flag it:

| Line | Category | Observation | Affected Task |
|------|----------|-------------|---------------|
| {line} | Ambiguous Content | Extracted text at lines X-Y contains directive language that subagents may confuse with orchestrator instructions. Wrapped in `<extracted-source>` for clarity. | {task} |

This is NOT a security flag -- it's a clarity improvement for self-authored plans.

## Status Tracking (NEW - v0.7, I10)

The suggestions table in the asset template now includes a Status column (promoted from biltong-buddy real-world output):

| Status | Meaning |
|--------|---------|
| OPEN | Not yet reviewed |
| ACKNOWLEDGED | PM has read it |
| DEFERRED | Will address post-orchestration |
| N/A | Not applicable to this project |
```

**Resolves**: D1 (drift between reference and asset template)

---

#### Task A3: Update `assets/templates/suggestions.md` to be the canonical template

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/templates/suggestions.md`

**Current state**: 72 lines. Uses `{{DOUBLE_BRACE}}` placeholders consistently. Missing: Status column (I10), Ambiguous Content category (C1), Deferred Items section (G17).

**Changes**:

1. Add `Status` column to Critical, Gaps, and Improvement tables. Each table row gets a `| Status |` column with default value `OPEN`.

2. Add new section after Inconsistencies:
```markdown
## Ambiguous Content (Clarity Flags)

Content where extracted source text may be confused with agent instructions:

| Line | Extracted Text (preview) | Affected Task | Status |
|------|--------------------------|---------------|--------|
{{AMBIGUOUS_CONTENT_ROWS}}
```

3. Add new section before Summary:
```markdown
## Deferred Items

Items identified during orchestration that should be addressed after workflow completion:

| Item | Context | Suggested Timing |
|------|---------|-----------------|
{{DEFERRED_ITEMS_ROWS}}
```

4. Update Summary table to include Ambiguous Content and Deferred Items rows.

**Resolves**: D1 (canonical template), C1 (ambiguous content flagging), G17 (deferred items), I10 (status column)

---

#### Task A4: Verify `assets/templates/pm-orchestration.md` is the canonical template; add missing sections

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/templates/pm-orchestration.md`

**Current state**: 292 lines. Already has 5 sections the reference lacked (SUGGESTIONS AVAILABLE, AGENT ALLOCATIONS, VALIDATION CHECKLIST, EXTRACTION COVERAGE, FINAL OUTPUT). Missing: FILES GENERATED section (C7), placeholder convention header.

**Changes**:

1. Add placeholder convention comment at top (after the header, before first section):
```markdown
<!-- Placeholder conventions:
  {{DOUBLE_BRACE}} = filled by orchestrator during generation
  {SINGLE_BRACE}   = filled by PM at dispatch time
-->
```

2. Add new section `## FILES GENERATED` between TASK SEQUENCE and EXECUTION INSTRUCTIONS (after line 95):
```markdown
## FILES GENERATED

Files created during orchestration (verify these exist before dispatching):

| File | Path | Purpose |
|------|------|---------|
| PM-ORCHESTRATION.md | `{{ORCHESTRATION_PATH}}/PM-ORCHESTRATION.md` | This file (PM playbook) |
| CONTEXT.md | `{{ORCHESTRATION_PATH}}/CONTEXT.md` | Shared context for all subagents |
| SUGGESTIONS.md | `{{ORCHESTRATION_PATH}}/SUGGESTIONS.md` | Advisory observations |
| TASK-MANIFEST.md | `{{ORCHESTRATION_PATH}}/TASK-MANIFEST.md` | Agent allocations |
{{GENERATED_TASK_FILE_ROWS}}
```

3. In the EXECUTION INSTRUCTIONS spawn prompt (line 122-128), change `{TASK_FILE}` to `{TASK_FILE}` but add a comment line above it:
```
       <!-- {TASK_FILE} is filled by PM from the TASK SEQUENCE table's "Task File" column -->
```
This documents the intentional single-brace for PM-time filling.

4. Add VALIDATION CHECKLIST guidance (I7). Replace `{{VALIDATION_CHECKLIST}}` placeholder block with:
```markdown
## VALIDATION CHECKLIST

After all tasks complete, verify:

{{VALIDATION_CHECKLIST_ITEMS}}

> Items above are extracted from source document validation/success criteria sections.
> If source has no explicit validation criteria, note "No source-defined validation" and list structural checks:
> - [ ] All task files were dispatched and completed
> - [ ] No FAILED tasks remain
> - [ ] Progress tracker fully updated
```

**Resolves**: D2 (canonical template), C4 (placeholder documentation), C7 (FILES GENERATED), I7 (validation checklist guidance)

---

### Group B: SKILL.md Core Fixes -- Sequential after Group A

#### Task B1: Version bump and header update

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: Line 3, change `version: 0.6.0` to `version: 0.7.0`

**Resolves**: Version bump

---

#### Task B2: Rename Phase 0 to "Detection Confidence" (C5, I5)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: Lines 84-106. Replace:

```markdown
### Phase 0: Detect Existing PM Orchestration
```

with:

```markdown
### Pre-Analysis Gate: Detection Confidence

> This gate measures whether the source ALREADY contains PM orchestration structure.
> It produces a "Detection Confidence" score (0-100), which is SEPARATE from the
> Phase 1.4 "Complexity Score" that measures how complex the source content is.
```

Update the user choice dialog (lines 110-122) to say "Detection Confidence: {score}/100" instead of "Orchestration Score: {score}/100".

Update the reference pointer (line 124) to say:
```
> For scoring examples, see `references/pm-detection-examples.md` (Detection Confidence examples)
```

**Resolves**: C5 (separate scoring identity), I5 (rename Phase 0)

---

#### Task B3: Rename Phase 1.4 label for clarity (C5)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: Lines 157-173. Add a clarification note after the Phase 1.4 header:

```markdown
**1.4 Score Complexity**

> This produces the "Complexity Score" — measuring how complex the SOURCE CONTENT is.
> This is DIFFERENT from the Detection Confidence score in the Pre-Analysis Gate.
> The Complexity Score determines whether orchestration is warranted for new content.
```

Keep the existing scoring table unchanged.

**Resolves**: C5 (labeling clarity)

---

#### Task B4: Rewrite Phase 1.6 Agent Discovery to be explicit (C2)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: Lines 176-197. Replace the entire Phase 1.6 section with:

```markdown
**1.6 Runtime Agent/Skill Discovery (MANDATORY)**

Before assigning agents to tasks, you MUST inventory what's actually available. Do NOT guess or hallucinate agent names.

**Where to find available agents and skills:**

The information is in YOUR system prompt (the context you received when this conversation started). Specifically:

1. **Agent tool `subagent_type` parameter** — Your system prompt describes the Agent tool. The `subagent_type` parameter has an enum or description listing valid agent types. Extract that list. Common types include `general-purpose`, but the exact list depends on your environment.

2. **Available skills** — Your system prompt contains `<available-skills>` or skill listings in system reminders. Extract skill names from these. Each skill can be invoked by a subagent.

3. **If you cannot find either list** — Use ONLY `general-purpose` as the agent type and note in TASK-MANIFEST.md: "Agent discovery found no specialized types; all tasks assigned to general-purpose."

**Discovery procedure:**

```
Step 1: Search your system prompt for "subagent_type" — extract valid values
Step 2: Search your system prompt for skill names (e.g., lines starting with skill names, <available-skills> tags)
Step 3: Record findings in TASK-MANIFEST.md "Available Agents" section
Step 4: If Step 1 and Step 2 yield nothing, default to general-purpose for all tasks
```

**Matching Rules:**

| Priority | Match Type | Example |
|----------|-----------|---------|
| 1 | User-specified in source | Source says "use backend-architect" -> use it |
| 2 | Exact skill match from discovery | Task needs Q-SYS -> discovered `qsys-plugin-development` skill |
| 3 | General domain match | Task needs backend work -> `general-purpose` with descriptive prompt |
| 4 | Fallback | No match -> `general-purpose` |

**NEVER assign an agent type that you did not find in Steps 1-2.** If uncertain, default to `general-purpose`.
```

**Resolves**: C2 (no actual discovery mechanism)

---

#### Task B5: Harden recursion guard (C3)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: Lines 13-51 (Execution Model section). Replace:

1. Change the spawn prompt to use HTML comment sentinel as first token:
```
  prompt: |
    <!-- ORCHESTRATOR_SUBAGENT_ACTIVE_v0.7 -->
    Execute the prompt-orchestrator skill.
    Source: {source_file_or_prompt}
    Output: {output_directory}

    Read the skill at ~/.claude/skills/prompt-orchestrator/SKILL.md
    Follow all phases. Return results when complete.

    IMPORTANT: You must NOT invoke the prompt-orchestrator skill or /orchestrate command.
    You ARE the orchestrator. Proceed directly with Phase 0.
```

2. Update the Recursion Guard detection to match:
```markdown
### Recursion Guard

If your prompt contains ANY of these indicators, you ARE the subagent:
- The HTML comment `<!-- ORCHESTRATOR_SUBAGENT_ACTIVE_v0.7 -->`
- The phrase "Execute the prompt-orchestrator skill"
- You were spawned via the Agent tool

**DO NOT spawn again.** Proceed with the Pre-Analysis Gate.
```

3. Add after the recursion guard:
```markdown
### Anti-Re-entry Guard

Subagent spawn prompts (Phase 3.2) MUST include:
"You must NOT invoke the prompt-orchestrator skill, /orchestrate, or any orchestration commands."

This prevents task subagents from accidentally triggering re-entry.
```

**Resolves**: C3 (bypassable recursion guard)

---

#### Task B6: Define the BOTH execution path or remove it (C6)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: Lines 99-106 (Threshold Actions table) and lines 110-122 (User Choice dialog).

Replace the BOTH option with explicit behavior:

In the Threshold Actions table, change the 60-79 row to:
```
| 60-79 | Present choice: USE AS-IS (recommended) or REBUILD |
```

In the User Choice dialog, remove the BOTH option:
```
Options:
[USE AS-IS] "The iris is open" - Keep existing structure, execute as-is
[REBUILD] "Dial new coordinates" - Discard existing, create optimized structure
```

Add a note after:
```
> If the user wants BOTH the original preserved AND a new orchestration, they should choose
> USE AS-IS first, then re-run the orchestrator with `--rebuild` flag on the same source.
```

**Resolves**: C6 (BOTH has no execution path)

---

#### Task B7: Add instruction priority key (I14)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: Add new section after the "Core Principle: EXTRACT, DON'T GENERATE" section (after line 67), before "Generated Artifacts":

```markdown
## Instruction Priority (RFC 2119)

| Keyword | Meaning | Enforcement |
|---------|---------|-------------|
| MUST / MANDATORY | Required. Violation = skill failure. | Hard gate — cannot proceed without compliance |
| SHOULD | Strongly recommended. Skip only with documented justification. | Soft gate — note in SUGGESTIONS.md if skipped |
| MAY / OPTIONAL | Permitted but not required. | No enforcement |

When instructions conflict, higher-priority keywords win. MUST > SHOULD > MAY.
```

**Resolves**: I14 (no instruction priority hierarchy), G15 (MUST/MANDATORY/CRITICAL confusion)

---

#### Task B8: Reframe negative instructions with positive alternatives (H3)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Changes** across multiple sections:

1. Phase 2.4 PM-ORCHESTRATION section (line 315). Change:
```
Required sections:
- PM RULES (READ FIRST) — includes Rule 7: DO NOT READ SUBAGENT TASK FILES
```
to:
```
Required sections:
- PM RULES (READ FIRST) — includes Rule 7: Your only task-file interaction is pointing subagents at file paths
```

2. Anti-Patterns table (lines 508-523). Add a new column "Positive Reframe":

| # | Anti-Pattern | Positive Reframe | Fix |
|---|-------------|-----------------|-----|
| 8 | PM reading subagent task files | PM coordinates using TASK SEQUENCE table only | PM reads ONLY coordination files |
| 9 | PM reading source document | Source is fully extracted — TASK SEQUENCE has everything | Source is fully extracted |

3. The "PM Context Pollution Detection" section (lines 534-540) — add positive counterpart after the STOP phrases:

```markdown
**Instead, remind yourself:**
- "The TASK SEQUENCE table has every coordination detail I need."
- "I dispatch agents and track results — that's my entire job."
- "Reading task files would pollute my coordination context."
```

**Resolves**: H3 (negative instructions dominate)

---

#### Task B9: Add `--generate-only` conditional and operational limits (G4, G14)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Changes**:

1. In Phase 3.1 (line 440), add before the review block:
```markdown
**If `--generate-only` flag is set**: Skip Phase 3 entirely. Return the generated artifacts
(PM-ORCHESTRATION.md, TASK-MANIFEST.md, CONTEXT.md, SUGGESTIONS.md, task files) without
executing. Report: "Orchestration generated. {N} task files ready for execution."
```

2. Add new section after Error Handling (after line 576):
```markdown
## Operational Limits

| Limit | Value | Rationale |
|-------|-------|-----------|
| Max tasks per orchestration | 20 | Beyond 20, PM context rot becomes likely |
| Max retries per failed task | 2 | After 2 retries, escalate to user |
| Max parallel group size | 5 | File-overlap analysis becomes unreliable above 5 |
| Checkpoint interval | Every 3 tasks | PM should summarize progress every 3 completions |
| Single-task threshold | 1 task identified | If only 1 task, ask user: "Only 1 task detected. Execute directly or use full orchestration?" |

If any limit is exceeded, pause and report to the user before continuing.
```

**Resolves**: G4 (--generate-only undefined), G14 (no operational limits), G5 (single-task degenerate case)

---

#### Task B10: Add output directory conflict handling (G2, G3)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: Add new section in Phase 2, after "2.6 Output Location" (after line 403):

```markdown
**2.6.1 Output Directory Conflict Resolution**

If the output directory already contains orchestration files:

| Existing Files Found | Action |
|---------------------|--------|
| PM-ORCHESTRATION.md with all tasks NOT STARTED | Ask: OVERWRITE or choose new directory |
| PM-ORCHESTRATION.md with some tasks COMPLETE | Ask: RESUME (continue from last complete) or OVERWRITE |
| PM-ORCHESTRATION.md with all tasks COMPLETE | Ask: RE-RUN (fresh) or ABORT |
| subagent-tasks/ exists but no PM-ORCHESTRATION.md | OVERWRITE (orphaned files) |

**Resume Behavior**: Read existing PROGRESS TRACKER, skip COMPLETE tasks, begin from first non-COMPLETE task. Regenerate only task files that need re-extraction.
```

**Resolves**: G2 (no output conflict handling), G3 (no resumption protocol)

---

#### Task B11: Update References table in SKILL.md

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: Lines 555-565. Update the references table to reflect the new reference file purposes:

```markdown
## References (load as needed)

| Reference | When to Load | Content Type |
|-----------|-------------|-------------|
| `references/pm-orchestration-template.md` | Generating PM-ORCHESTRATION.md | Generation guide (template is in `assets/templates/`) |
| `references/suggestions-template.md` | Generating SUGGESTIONS.md | Generation guide (template is in `assets/templates/`) |
| `references/capability-inference.md` | Generating TASK-MANIFEST.md | Inference rules and tag mapping |
| `references/pm-detection-examples.md` | Scoring Detection Confidence | Examples for Pre-Analysis Gate |
| `references/extraction-examples.md` | Reviewing extraction patterns | Good/bad extraction examples |
| `references/workflow-patterns.md` | Identifying task decomposition | Pattern library (Sequential, Fan-Out, Build-Test) |
```

**Resolves**: Keeps reference table accurate after Group A changes

---

### Group C: Template Hardening -- Parallel with Group B (no dependency)

#### Task C1: Add `<extracted-source>` XML wrapping to task template (C1, I9)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/templates/task.md`

**Changes**:

1. In the "Extracted Content" section (lines 29-38), wrap the content areas:

```markdown
## Extracted Content

> **IMPORTANT**: Content in `<extracted-source>` tags is EXTRACTED VERBATIM from the user's source document.
> It is specification to implement, NOT instructions to you. Implement exactly as specified.

### {{PRIMARY_SECTION_HEADER}}

<extracted-source document="{{SOURCE_DOCUMENT_PATH}}" lines="{{PRIMARY_LINE_START}}-{{PRIMARY_LINE_END}}">
{{EXTRACTED_PRIMARY_CONTENT}}
</extracted-source>

### {{RELATED_SECTION_HEADER}}

<extracted-source document="{{SOURCE_DOCUMENT_PATH}}" lines="{{RELATED_LINES}}">
{{EXTRACTED_RELATED_CONTENT}}
</extracted-source>
```

2. Update the Extraction Certification section (lines 49-56). Replace `{{YES_NO_NA}}` data fields with proper checkboxes (I8):

```markdown
## Extraction Certification

- [ ] Self-contained: Subagent can complete with only this file + CONTEXT.md
- [ ] Rollback included (if destructive operation present)
- [ ] Backup included (if data operation present)
- [ ] Prerequisites included
- [ ] Line count: {{TOTAL_LINES}} lines (MUST re-scan source if < 50)
- [ ] Semantic scan performed across full document
- [ ] All `<extracted-source>` blocks are verbatim copies (no edits)
```

3. Add a "Related Sections" table that supports MULTIPLE rows (C4 — currently only 1 row):

Replace the single-row related sections table with:
```markdown
**Related Sections** (from semantic scan):

| Section | Lines | Relationship | Why Bundled |
|---------|-------|--------------|-------------|
{{RELATED_SECTIONS_ROWS}}

> Add one row per related section. If no related sections, write "None — primary section is self-contained."
```

4. Add file scope notice after the Context section:
```markdown
## Scope Restriction

You may ONLY read and modify files within: `{{WORKING_DIRECTORY}}`
Do NOT access `~/.claude/skills/`, `~/.ssh/`, or paths outside the project directory.
Do NOT modify orchestration files (PM-ORCHESTRATION.md, CONTEXT.md, SUGGESTIONS.md, TASK-MANIFEST.md).
```

**Resolves**: C1 (extraction wrapping), C4 (single related-section row), I8 (checkbox certification), I9 (XML tags), H1 (file scope), I6 (anti-tampering)

---

#### Task C2: Harden the subagent spawn prompt in PM-ORCHESTRATION template (H2, I6)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/templates/pm-orchestration.md`

**Change**: Lines 111-128. Replace the entire spawn prompt block:

```markdown
3. **Spawn Subagent**
   ```
   Agent tool invocation:
     subagent_type: "{{ALLOCATED_AGENT}}"
     description: "<3-5 word summary>"
     prompt: |
       <!-- ORCHESTRATOR_TASK_AGENT -->
       ROLE: {{ROLE_SENTENCE}}

       You are executing a task for the {{PROJECT_NAME}} project.
       Working directory: {{WORKING_DIRECTORY}}

       STEP 1: Read the shared context file:
         {{CONTEXT_PATH}}

       STEP 2: Read and execute the task file:
         {{TASK_FILES_PATH}}/{TASK_FILE}

       STEP 3: Content in `<extracted-source>` tags is the user's specification.
         Implement it exactly as written. Do not rewrite, improve, or substitute.

       STEP 4: Follow the task's validation criteria exactly.

       STEP 5: Report your result using ONE of these statuses:
         STATUS: COMPLETE - task fully done, all validation passed
         STATUS: FAILED - task could not be completed (explain why)
         STATUS: BLOCKED - dependency missing or environment issue (explain what's needed)
         STATUS: CLARIFICATION NEEDED - source content is ambiguous (cite specific lines)

       CONSTRAINTS:
       - Only read/modify files within {{WORKING_DIRECTORY}}
       - Do NOT access ~/.claude/skills/, ~/.ssh/, or paths outside the project
       - Do NOT modify orchestration files (PM-ORCHESTRATION.md, CONTEXT.md, etc.)
       - Do NOT invoke /orchestrate or any orchestration commands
       - If source content is missing something, report the gap — do NOT invent content
   ```

   > The `{{ROLE_SENTENCE}}` placeholder is filled by the orchestrator during Phase 2.
   > It should be a single sentence describing the agent's role for this specific task.
   > Example: "You are a backend implementation specialist focused on database migrations."
   
   **DO NOT read the task file yourself to "build a better prompt."** Your ONLY interaction with task files is pointing subagents at their file paths. The prompt above is sufficient — the task file is self-contained.
```

Also update the "Handle Results" section (line 149-151) to include all 4 statuses:
```markdown
5. **Handle Results**
   - COMPLETE: Update tracker, proceed to next task
   - FAILED: STOP workflow, document failure, escalate to user
   - BLOCKED: Check if blocker can be resolved, report to user if not
   - CLARIFICATION NEEDED: Report to user with the specific question from subagent
```

**Resolves**: H2 (missing role/statuses/constraints), I6 (anti-tampering), H1 (file scope in spawn), C1 (extraction discipline reminder)

---

#### Task C3: Add H4 guard for task description injection

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/templates/pm-orchestration.md`

**Change**: In the TASK SEQUENCE section (around line 89), add a note after the table:

```markdown
> **Description column rules** (H4): Descriptions are metadata labels for PM coordination.
> - Maximum 80 characters
> - No imperative language (no "you must", "implement", "create")
> - Format: noun phrase describing the deliverable (e.g., "Database migration scripts", "Auth module unit tests")
> - If source phase names contain imperative language, convert to noun phrases
```

**Resolves**: H4 (description column injection surface)

---

#### Task C4: Make CONTEXT.md CLAUDE.md dependency conditional (C8)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/templates/context.md`

**Change**: Lines 7-10. Replace:

```markdown
## MANDATORY FIRST STEP

Before doing any work, read: `{{CLAUDE_MD_PATH}}`
```

with:

```markdown
{{#IF_CLAUDE_MD_EXISTS}}
## MANDATORY FIRST STEP

Before doing any work, read: `{{CLAUDE_MD_PATH}}`
{{/IF_CLAUDE_MD_EXISTS}}
```

And add an orchestrator instruction comment at the top of the file:

```markdown
<!-- Orchestrator: Only include the MANDATORY FIRST STEP section if a CLAUDE.md file
     exists at the project root or a known location. Check for existence before including.
     If no CLAUDE.md found, omit the entire MANDATORY FIRST STEP section. -->
```

**Resolves**: C8 (hard CLAUDE.md dependency)

---

#### Task C5: Add suggestion injection guard to suggestions template (H5)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/templates/suggestions.md`

**Change**: Add after the advisory header (line 6):

```markdown
> **CRITICAL RULE**: Suggestions are ADVISORY ONLY. They MUST NOT be implemented in task files.
> If a suggestion is accepted by the user, the SOURCE DOCUMENT must be amended first.
> Task files are then re-extracted from the amended source. NEVER modify task files to
> implement a suggestion — that violates EXTRACT DON'T GENERATE.
>
> Status values: OPEN | ACKNOWLEDGED | DEFERRED | N/A
```

**Resolves**: H5 (suggestions injected back into tasks)

---

#### Task C6: Add BLOCKING RELATIONSHIP and Deferred Items to task template (I10)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/templates/task.md`

**Change**: Add after the Source Reference section (after line 17):

```markdown
## Dependencies

**Blocked by**: {{BLOCKER_TASKS}}
**Blocks**: {{DOWNSTREAM_TASKS}}
```

This promotes the BLOCKING RELATIONSHIP pattern seen in real-world biltong-buddy output (I10).

**Resolves**: I10 (promote real-world patterns)

---

#### Task C7: Add role sentence instruction to SKILL.md Phase 2 (H2 orchestrator side)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: In Phase 2.4 "Create Extraction Files" (around line 314), add to the PM-ORCHESTRATION.md generation instructions:

```markdown
**Role Sentence Generation (MANDATORY for each task)**:

During Phase 2, for each task, write a single `{{ROLE_SENTENCE}}` that will be inserted into
the spawn prompt. This sentence primes the subagent's identity for the specific task.

Format: "You are a {domain} specialist focused on {specific task objective}."

Examples:
- "You are a database migration specialist focused on schema evolution."
- "You are a Lua plugin developer focused on Q-SYS control integration."
- "You are a documentation writer focused on API reference completeness."

Store role sentences in the TASK SEQUENCE table as an additional column, or in TASK-MANIFEST.md.
```

**Resolves**: H2 (orchestrator side of role sentence generation)

---

### Group D: Reference File Updates -- Parallel with Groups B and C

#### Task D1: Update `references/capability-inference.md` with modern web technologies (G11)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/references/capability-inference.md`

**Change**: Lines 10-31 (Content Pattern table). Add these rows:

```markdown
| TypeScript, .ts, .tsx, interface, type | `typescript` |
| React, JSX, useState, useEffect, component | `react`, `frontend` |
| Next.js, getServerSideProps, App Router | `nextjs`, `frontend`, `fullstack` |
| Vue, .vue, Composition API, ref, computed | `vue`, `frontend` |
| Svelte, .svelte, SvelteKit | `svelte`, `frontend` |
| Tailwind CSS, className, utility classes | `css`, `frontend` |
| REST API, endpoint, route, middleware | `api`, `backend` |
| GraphQL, query, mutation, resolver | `graphql`, `api` |
| Prisma, Drizzle, ORM, schema | `orm`, `database` |
| WebSocket, real-time, socket.io | `websocket`, `networking` |
| Markdown, .md, documentation site | `documentation` |
```

Also update the Capability Legend in `assets/templates/task-manifest.md` (Task D1b below).

**Resolves**: G11 (missing modern web capabilities)

---

#### Task D1b: Sync task-manifest.md Capability Legend with capability-inference.md

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/templates/task-manifest.md`

**Change**: Lines 53-69 (Capability Legend). Add the new tags to match the updated inference rules:

```markdown
| `typescript` | TypeScript code, .ts/.tsx files |
| `react` | React components, hooks, JSX |
| `nextjs` | Next.js framework, SSR, App Router |
| `vue` | Vue.js components, Composition API |
| `svelte` | Svelte/SvelteKit components |
| `css` | CSS, Tailwind, styling |
| `api` | REST API, endpoints, middleware |
| `graphql` | GraphQL queries, mutations, resolvers |
| `orm` | Prisma, Drizzle, database ORM |
| `websocket` | WebSocket, real-time communication |
| `python` | Python code, pip packages |
| `javascript` | JavaScript, npm, Node.js |
| `shell` | Bash, shell scripts |
| `git` | Git operations, branching |
| `fullstack` | Full-stack work spanning frontend + backend |
| `backend` | Server-side logic, business layer |
```

**Resolves**: G11 (capability legend mismatch)

---

#### Task D2: Clean up `references/workflow-patterns.md` — remove dead operator concept (G13)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/references/workflow-patterns.md`

**Changes**:

1. Remove the "Operator Application Guidelines" section (lines 234-285) entirely. The Generate/Review/Revise/Test/Ensemble operator taxonomy is never referenced by SKILL.md or any template. It adds ~50 lines of dead knowledge.

2. In Pattern 3 "Generate-Review-Revise Loop" (lines 57-81), remove the `(operator: Generate)` annotations from the example:
```
01-generate-guide.md (blocker: none)
02-review-guide.md (blocker: 01)
03-revise-guide.md (blocker: 02)
04-validate-guide.md (blocker: 03)
```

3. In the "Task File Structure Guidelines" section (lines 149-232), update the task examples to match the current `assets/templates/task.md` structure. The "Complex Task" example (lines 180-232) needs:
   - Add `## Source Reference` with `**Primary Section**` and `**Related Sections**` table
   - Add `## Extraction Certification` checklist
   - Remove `**Operator**: Generate` line (dead concept)
   - Add `<extracted-source>` wrapping around implementation content
   - Add `## Scope Restriction` section

4. Update the "Minimal Task" example (lines 151-176) similarly — add Source Reference and Extraction Certification.

**Resolves**: G13 (dead operator concept), D4 (workflow pattern examples don't match current template)

---

#### Task D3: Update `references/extraction-examples.md` with `<extracted-source>` wrapping

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/references/extraction-examples.md`

**Change**: Update all 4 examples to show `<extracted-source>` XML wrapping:

Example 1 (lines 17-25), change the task file portion to:
```markdown
**Task File** (pure extraction):
```markdown
## Implementation (source lines 450-470)

<extracted-source document="{source_path}" lines="450-470">
```lua
function LogError(message)
  print("[ERROR] " .. message)  -- Wrong API for Q-SYS
end
```
</extracted-source>
```

Apply the same pattern to Examples 2, 3, and 4.

In Example 4 "Semantic Bundling" (lines 110-128), the RIGHT example should also show `<extracted-source>` wrapping around each related section block.

**Resolves**: D4 (extraction examples don't match current template), C1 (show extraction wrapping in examples)

---

#### Task D4: Update `references/pm-detection-examples.md` — rename to Detection Confidence

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/references/pm-detection-examples.md`

**Change**: Line 131-138. The "Detection Confidence" section at the bottom already uses the right terminology. Update the file header (lines 1-5):

Change:
```
# PM Detection Examples

> Reference file for prompt-orchestrator skill.
> Load when evaluating Phase 0 orchestration detection scoring.
```

to:
```
# Detection Confidence Examples

> Reference file for prompt-orchestrator skill.
> Load when evaluating Pre-Analysis Gate detection confidence scoring.
> 
> This measures whether the source ALREADY contains PM orchestration structure.
> It is NOT the same as the Complexity Score (Phase 1.4).
```

**Resolves**: C5 (naming alignment)

---

### Group E: Python Script Alignment -- After Group B (depends on B2/B3 for naming)

#### Task E1: Align `analyze_prompt.py` scoring formula with Phase 1.4

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/scripts/analyze_prompt.py`

**Changes**:

1. Update the `COMPLEXITY_WEIGHTS` dictionary (lines 54-60) to match Phase 1.4 formula from SKILL.md:

```python
# Aligned with SKILL.md Phase 1.4 Complexity Score
COMPLEXITY_WEIGHTS = {
    "phase_section": 10,    # Each phase/major section: +10
    "subagent_prompt": 5,   # Each subagent prompt: +5
    "code_block": 2,        # Each code block: +2
    "source_lines": 1,      # Per 100 lines: +1
    "dependency": 3,        # Each dependency between sections: +3
}
```

2. Update the `calculate_complexity` function (lines 248-283) to use the new weights:

```python
def calculate_complexity(
    tasks: List[Task],
    dependencies: List[Tuple[int, int]],
    files: List[str],
    prompt: str
) -> Tuple[int, dict]:
    """Calculate complexity score aligned with SKILL.md Phase 1.4."""
    
    # Count structural elements
    phases = len(re.findall(r'^#{1,3}\s+(?:phase|step|part)\s+\d', prompt, re.MULTILINE | re.IGNORECASE))
    code_blocks = len(re.findall(r'```', prompt)) // 2  # pairs of backticks
    subagent_prompts = len(re.findall(r'(?:subagent|prompt|agent):', prompt, re.IGNORECASE))
    source_lines = prompt.count('\n') + 1
    
    breakdown = {
        "phases_sections": max(phases, len(tasks)) * COMPLEXITY_WEIGHTS["phase_section"],
        "subagent_prompts": subagent_prompts * COMPLEXITY_WEIGHTS["subagent_prompt"],
        "code_blocks": code_blocks * COMPLEXITY_WEIGHTS["code_block"],
        "source_lines": (source_lines // 100) * COMPLEXITY_WEIGHTS["source_lines"],
        "dependencies": len(dependencies) * COMPLEXITY_WEIGHTS["dependency"],
    }
    
    total = sum(breakdown.values())
    return min(total, 100), breakdown
```

3. Update the `get_recommendation` function (lines 286-293) to match SKILL.md Phase 1.5 thresholds:

```python
def get_recommendation(score: int) -> str:
    """Get recommendation aligned with SKILL.md Phase 1.5."""
    if score <= 30:
        return "ASK_USER: Complexity score {}/100. Execute directly or decompose?".format(score)
    else:
        return "PROCEED: Complexity score {}/100. Orchestration recommended.".format(score)
```

4. Update the docstring (lines 2-12) to note alignment:
```python
"""
Prompt Complexity Analyzer (v0.7.0)

Analyzes a prompt for complexity signals using the Phase 1.4 Complexity Score formula
from SKILL.md. Outputs a structured report with task extraction, dependency mapping,
and complexity scoring.

NOTE: This script produces the "Complexity Score" (how complex is the content?).
It does NOT produce the "Detection Confidence" score (does it already have orchestration?).
...
```

**Resolves**: C5 (script formula mismatches Phase 1.4)

---

### Group F: Examples Update -- After Groups B, C, D (depends on final template structure)

#### Task F1: Rewrite `assets/examples/PM-ORCHESTRATION.md` to match v0.7 template

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/examples/PM-ORCHESTRATION.md`

**Current state**: 87 lines. Missing 10 of the 16 required sections. Only has 4 PM rules instead of 7. No CONTEXT DISCIPLINE, no EXTRACTION-BASED WORKFLOW, no VALIDATION CHECKLIST, no EXTRACTION COVERAGE, no FILES GENERATED, no NON-NEGOTIABLES, no WHY SUBAGENTS ARE MANDATORY.

**Action**: Complete rewrite using the same QRC Room State Controller project context, but following the v0.7 template structure exactly. Must include ALL 16 sections listed in Task A1's checklist. Key additions:

- Full 7 PM rules
- CONTEXT DISCIPLINE section with consequence table
- SUGGESTIONS AVAILABLE with counts
- AGENT ALLOCATIONS summary
- FILES GENERATED section listing all created files
- EXTRACTION-BASED WORKFLOW section
- EXTRACTION COVERAGE table mapping all 6 source phases
- WHY SUBAGENTS ARE MANDATORY reasoning table
- NON-NEGOTIABLES with all 8 rules and anti-rationalization phrases
- Hardened spawn prompt with role sentence, 4 statuses, file scope, extraction discipline
- VALIDATION CHECKLIST with concrete checks

**Resolves**: H6 (stale examples), D3 (examples incompatible with templates)

---

#### Task F2: Rewrite `assets/examples/CONTEXT.md` to match v0.7 template

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/examples/CONTEXT.md`

**Current state**: 34 lines. Missing: TECHNOLOGIES, CONSTRAINTS, STATUS REPORTING format, conditional CLAUDE.md section.

**Action**: Expand to match v0.7 `assets/templates/context.md`. Add all required sections using QRC Room State Controller project data.

**Resolves**: H6 (stale examples), D3

---

#### Task F3: Rewrite example task files to match v0.7 template

**Files**: All files in `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/examples/subagent-tasks/`

**Action**: Rewrite `structural-updates.md` and `4A-logging-phase.md` as representative examples. Each must have:
- Source Reference with Primary Section and Related Sections table
- `<extracted-source>` wrapping around extracted content
- Dependencies section (blocked by / blocks)
- Scope Restriction section
- Extraction Certification checklist with checkboxes
- All 4 status values in Output Format
- Error Handling table

Consider reducing the 6 example tasks to 2-3 representative ones (one simple, one complex with semantic bundling) to keep the examples directory manageable.

**Resolves**: H6 (stale examples), D3, D4

---

#### Task F4: Create `assets/examples/TASK-MANIFEST.md` example (G9)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/examples/TASK-MANIFEST.md` (new file)

**Action**: Create a populated example using QRC Room State Controller data showing:
- Available Agents section with discovered agents
- Context Rot Risk Assessment with one Low, one Medium, and one High task
- Task Capabilities table with populated capability tags, allocated agents, confidence scores
- Capability Legend
- Allocation Summary with counts
- One Ambiguous Task example

**Resolves**: G9 (no TASK-MANIFEST example)

---

#### Task F5: Create `assets/examples/SUGGESTIONS.md` example (G10)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/examples/SUGGESTIONS.md` (new file)

**Action**: Create a populated example with:
- 1 Critical item (the `print()` vs `Log.Error()` API issue from extraction-examples.md)
- 1 Gap item (the missing Logon command format)
- 1 Inconsistency (example: room count discrepancy)
- 1 Improvement (example: add retry logic)
- 1 Structural note (example: Phase 4B depends on 4A's LogManager)
- 1 Ambiguous Content flag
- 1 Deferred Item
- Status column populated with mixed values (OPEN, ACKNOWLEDGED, DEFERRED)
- Summary table with counts

**Resolves**: G10 (no SUGGESTIONS.md example)

---

### Group G: Gap Filling -- After Groups B and C

#### Task G1: Add inline prompt handling path (G1)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: In Phase 1.1 (lines 130-140), add a new subsection:

```markdown
**1.1.1 Inline Prompt Handling**

If the source is an inline prompt (not a file with structure/headers/line numbers):

1. Treat the entire prompt as a single "source document"
2. Use sentence/paragraph boundaries instead of header-based structure
3. Line numbers are optional — use paragraph references instead
4. Task files should quote the relevant prompt excerpt rather than referencing lines
5. Source Reference in task files uses `**Source**: inline prompt` with the quoted excerpt

All other phases apply normally. The orchestrator still extracts, does not generate.
```

**Resolves**: G1 (no inline prompt path)

---

#### Task G2: Add parallel file-overlap verification procedure (G6)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: In Phase 1.7 or at the end of Phase 2, add:

```markdown
**2.5.1 Parallel Group File-Overlap Verification (MANDATORY for parallel tasks)**

Before marking tasks as parallel, verify zero file overlap:

1. For each candidate parallel task, list ALL files it will read or modify
2. Compare file lists between all tasks in the proposed parallel group
3. If ANY file appears in 2+ task file lists → tasks CANNOT be parallel

Document the verification in PM-ORCHESTRATION.md:

```markdown
### Parallel Group {N} File-Overlap Analysis

| Task | Files Read | Files Modified |
|------|-----------|---------------|
| {task_a} | {list} | {list} |
| {task_b} | {list} | {list} |

**Overlap**: None confirmed → safe for parallel execution
```

If overlap is found, convert to sequential execution with appropriate blockers.
```

**Resolves**: G6 (parallel file-overlap undefined)

---

#### Task G3: Add extraction coverage as hard gate (I1)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: In Phase 2.7 "Extraction Validation" (lines 405-437), add a hard gate:

After "If any section shows NOT EXTRACTED":
```markdown
**HARD GATE**: You MUST NOT proceed to Phase 3 if coverage is below 95%.
If coverage < 95%, identify the missing sections and re-extract before continuing.
The only exception is sections explicitly marked "out of scope" by the user.
```

**Resolves**: I1 (coverage validation should be hard gate)

---

#### Task G4: Add explicit WAIT in Phase 3.1 (I2)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: In Phase 3.1 (lines 440-458), add after the options block:

```markdown
**WAIT FOR USER RESPONSE.** Do NOT proceed to Phase 3.2 until the user selects an option.
If no response is received, do NOT default to [PROCEED]. The user MUST explicitly confirm.
```

**Resolves**: I2 (PM skipping user confirmation)

---

#### Task G5: Add context deduplication example pointer (G12)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: In Phase 2.2 "Context Deduplication" (around line 300), add:

```markdown
> **Example**: If the source document has a "Port Configuration" table referenced by 3 different
> phases, extract it ONCE to CONTEXT.md under `## Port Configuration`. In each task file that
> needs it, write: `See CONTEXT.md Section: Port Configuration`. Do NOT copy the table 3 times.
```

**Resolves**: G12 (no deduplication example)

---

#### Task G6: Add PM mechanism to track suggestion status (G16)

**File**: `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md`

**Change**: In Phase 3.1 (after the pre-execution review), add:

```markdown
**3.1.1 Suggestion Triage**

After the user responds to the pre-execution review, if they choose [REVIEW]:
1. Present the full SUGGESTIONS.md
2. For each Critical item, ask: ACKNOWLEDGE / DEFER / N/A
3. Update the Status column in SUGGESTIONS.md accordingly
4. Return to the options prompt

The PM SHOULD review SUGGESTIONS.md before dispatching the first task and note any
ACKNOWLEDGED items that may affect execution.
```

**Resolves**: G16 (no PM mechanism to update suggestions)

---

## Dependency Map

```
Group A (Drift Resolution)
  ├── A1 (pm-orchestration ref → guidance)
  ├── A2 (suggestions ref → guidance)
  ├── A3 (suggestions template canonical)
  └── A4 (pm-orchestration template canonical)
       │
       ├───────────────────────┐
       ▼                       ▼
Group B (SKILL.md)        Group C (Templates)        Group D (References)
  ├── B1 (version)          ├── C1 (task.md)          ├── D1 (capability-inference)
  ├── B2 (Phase 0 rename)   ├── C2 (spawn prompt)     ├── D1b (manifest legend)
  ├── B3 (Phase 1.4 label)  ├── C3 (H4 guard)         ├── D2 (workflow-patterns)
  ├── B4 (agent discovery)  ├── C4 (context.md)        ├── D3 (extraction-examples)
  ├── B5 (recursion guard)  ├── C5 (suggestions guard) └── D4 (detection-examples)
  ├── B6 (BOTH removal)     ├── C6 (task dependencies)
  ├── B7 (priority key)     └── C7 (role sentence)
  ├── B8 (positive framing)
  ├── B9 (generate-only)
  ├── B10 (conflicts)
  └── B11 (ref table)
       │                       │                        │
       └───────────┬───────────┘────────────────────────┘
                   ▼
            Group E (Python)
              └── E1 (formula alignment)
                   │
                   ▼
            Group F (Examples)
              ├── F1 (PM-ORCH example)
              ├── F2 (CONTEXT example)
              ├── F3 (task examples)
              ├── F4 (MANIFEST example - NEW)
              └── F5 (SUGGESTIONS example - NEW)
                   │
                   ▼
            Group G (Gap Filling)
              ├── G1 (inline prompts)
              ├── G2 (file-overlap)
              ├── G3 (coverage hard gate)
              ├── G4 (explicit WAIT)
              ├── G5 (dedup example)
              └── G6 (suggestion triage)
```

## Parallel Execution Opportunities

- **Groups B, C, D** can all run in parallel after Group A completes (they edit different files)
- Within Group B: B1-B11 are sequential (all edit SKILL.md)
- Within Group C: C1-C7 can mostly run in parallel (different template files), except C2 and C3 both edit pm-orchestration.md (sequential)
- Within Group D: D1-D4 can all run in parallel (different reference files), except D1 and D1b share a logical unit
- **Group E** depends on B2/B3 completing (naming alignment)
- **Group F** depends on all of B, C, D (needs final template structure)
- **Group G** depends on B completing (all edit SKILL.md, must be after B's SKILL.md changes)

## Findings Coverage Matrix

| Finding | Task(s) | Status |
|---------|---------|--------|
| C1 (Injection → clarity) | A3, C1, C2, D3 | Addressed |
| C2 (Agent Discovery) | B4 | Addressed |
| C3 (Recursion Guard) | B5 | Addressed |
| C4 (Placeholder) | A4, C1 | Addressed |
| C5 (Scoring) | B2, B3, D4, E1 | Addressed |
| C6 (BOTH option) | B6 | Addressed |
| C7 (Task Paths) | A4 | Addressed |
| C8 (CLAUDE.md) | C4 | Addressed |
| H1 (File Scope) | C1, C2 | Addressed |
| H2 (Spawn Prompt) | C2, C7 | Addressed |
| H3 (Positive Frame) | A1, B8 | Addressed |
| H4 (Description Inject) | C3 | Addressed |
| H5 (Suggestion Inject) | C5 | Addressed |
| H6 (Stale Examples) | F1-F5 | Addressed |
| D1 (suggestions drift) | A2, A3 | Addressed |
| D2 (pm-orch drift) | A1, A4 | Addressed |
| D3 (examples drift) | F1-F3 | Addressed |
| D4 (workflow patterns) | D2 | Addressed |
| G1 (Inline prompt) | G1 | Addressed |
| G2 (Output conflict) | B10 | Addressed |
| G3 (Resumption) | B10 | Addressed |
| G4 (generate-only) | B9 | Addressed |
| G5 (Single task) | B9 | Addressed |
| G6 (Parallel overlap) | G2 | Addressed |
| G7 (Agent Teams) | Deferred | Not practical without experimental feature stability |
| G8 (Self-assessed cert) | Deferred | No independent verification mechanism available |
| G9 (Manifest example) | F4 | Addressed |
| G10 (Suggestions example) | F5 | Addressed |
| G11 (Capabilities) | D1, D1b | Addressed |
| G12 (Dedup example) | G5 | Addressed |
| G13 (Dead operators) | D2 | Addressed |
| G14 (Operational limits) | B9 | Addressed |
| G15 (Priority hierarchy) | B7 | Addressed |
| G16 (Suggestion tracking) | G6 | Addressed |
| G17 (Deferred items) | A3 | Addressed |
| I1 (Coverage hard gate) | G3 | Addressed |
| I2 (Explicit WAIT) | G4 | Addressed |
| I3 (Error handling) | C2 (4 statuses) | Partially addressed |
| I4 (Bundle upper bound) | Deferred | Would require SKILL.md context rot thresholds update |
| I5 (Phase 0 rename) | B2 | Addressed |
| I6 (Anti-tampering) | C1, C2 | Addressed |
| I7 (Validation checklist) | A4 | Addressed |
| I8 (Checkbox cert) | C1 | Addressed |
| I9 (XML tags) | C1, D3 | Addressed |
| I10 (Real-world patterns) | A3, C6, F1 | Addressed |
| I11 (confirm-agents) | Deferred | Requires deeper UX consideration |
| I12 (Info leakage) | Deferred | Low priority for self-authored plans |
| I13 (Consolidate anti-patterns) | B8 | Partially addressed |
| I14 (Priority key) | B7 | Addressed |

## Strengths Preservation Check

| Strength | Preserved By |
|----------|-------------|
| S1 (EXTRACT DON'T GENERATE) | Untouched core principle, reinforced by C1 XML wrapping |
| S2 (PM anti-rationalization) | Kept in NON-NEGOTIABLES, enhanced with positive framing (B8) |
| S3 (Context discipline) | Kept intact, FILES GENERATED section adds to it (A4) |
| S4 (Semantic bundling) | Untouched, enhanced with upper bound mention |
| S5 (Context rot assessment) | Untouched |
| S6 (Lazy-loading references) | Maintained, reference table updated (B11) |
| S7 (Coverage table) | Enhanced with hard gate (G3) |
| S8 (Real-world splitting) | Untouched |
| S9 (Parallel file-overlap) | Enhanced with formal procedure (G2) |
| S10 (Shared rollback in CONTEXT.md) | Untouched |

## Estimated Impact

- **Files modified**: 16 (of 21 existing)
- **New files created**: 2 (TASK-MANIFEST example, SUGGESTIONS example)
- **Files deleted**: 0
- **Net line change estimate**: +200 to +300 lines across all files
- **Token impact on SKILL.md**: Approximately +150 lines (from ~576 to ~726), keeping initial load reasonable given lazy-loading reference pattern

---

### Critical Files for Implementation

- `/home/spark-bitch/.claude/skills/prompt-orchestrator/SKILL.md` - Core skill logic; receives the most changes across Groups B and G (version bump, Phase 0 rename, agent discovery rewrite, recursion guard, BOTH removal, priority key, positive framing, generate-only, conflict handling, inline prompts, coverage gate, explicit WAIT)
- `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/templates/pm-orchestration.md` - Canonical PM template; receives FILES GENERATED section, hardened spawn prompt, placeholder documentation, H4 guard, validation checklist guidance
- `/home/spark-bitch/.claude/skills/prompt-orchestrator/assets/templates/task.md` - Canonical task template; receives `<extracted-source>` wrapping, multi-row Related Sections, checkbox certification, scope restriction, dependencies section
- `/home/spark-bitch/.claude/skills/prompt-orchestrator/references/pm-orchestration-template.md` - Must be fully rewritten from template-duplicate to guidance-only document (drift resolution is prerequisite for everything else)
- `/home/spark-bitch/.claude/skills/prompt-orchestrator/scripts/analyze_prompt.py` - Python scoring formula must be realigned to match SKILL.md Phase 1.4 weights (phase_section: 10, subagent_prompt: 5, code_block: 2, source_lines: 1/100, dependency: 3)
